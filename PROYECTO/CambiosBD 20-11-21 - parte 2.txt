ALTER TABLE TBL_SERVICIO_IMPUESTOS 
DROP CONSTRAINT TBL_SERVICIO_IMPUESTOS_PK;

ALTER TABLE TBL_SERVICIO_IMPUESTOS
ADD CONSTRAINT TBL_SERVICIO_IMPUESTOS_PK PRIMARY KEY 
(
  NO_CIA 
, CODIGO_SERVICIO 
)
ENABLE;

/

CREATE TABLE "SAFE"."TBL_FACTURA_AUTORIZACION" 
   (	"FACAU_FACTURA" NUMBER(11,0) NOT NULL ENABLE, 
	"FACAU_CLIENTE" NUMBER(11,0) NOT NULL ENABLE, 
	"FACAU_VENDEDOR" VARCHAR2(20 BYTE), 
	"FACAU_ADMINISTRADOR" VARCHAR2(20 BYTE), 
	"FACAU_DESCRIPCION" VARCHAR2(2000 BYTE), 
	"FACAU_TIPO" VARCHAR2(100 BYTE), 
	"FACAU_FECHA" DATE, 
	"NO_CIA" VARCHAR2(3 BYTE), 
	 CONSTRAINT "TBL_FACTURA_AUTORIZACION_PK1" PRIMARY KEY ("NO_CIA", "FACAU_FACTURA", "FACAU_TIPO", "FACAU_FECHA")
   )

/

ALTER TABLE TBL_FACTURA_AUTORIZACION
ADD CONSTRAINT TBL_FACTURA_AUTORIZACION_FK1 FOREIGN KEY
(
  NO_CIA 
)
REFERENCES TBL_EMPRESA
(
  NO_CIA 
)
ENABLE;


/

    create public synonym TBL_FACTURA_AUTORIZACION for SAFE.TBL_FACTURA_AUTORIZACION;
    
    grant all on TBL_FACTURA_AUTORIZACION to SAFE_USUARIOS;

	/

